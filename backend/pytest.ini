[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: tests that require a live Postgres/Redis (run in CI)
filterwarnings =
    ignore::DeprecationWarning
